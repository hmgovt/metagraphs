# Taostats free-tier API key. Used only by the daily snapshot workflow in CI;
# never read by the browser. Copy to .env locally; the real value lives in the
# repo's GitHub Actions secrets for scheduled runs.
TAOSTATS_API_KEY=
